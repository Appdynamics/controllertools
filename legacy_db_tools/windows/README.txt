Windows command shell scripts (.cmd files) for common database recovery tasks.

We have scripts to do the following:
* create db dumps with mysqldump
* load db dumps created with above script
* run arbitrary sql scripts
* display root mysql password
* manually upgrade db schema
